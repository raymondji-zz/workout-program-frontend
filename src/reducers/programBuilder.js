import objectAssign from 'object-assign';

const initialState = {
    days: []
};

export default function programBuilderStateState(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
